function colorChange() {
    var list = ["rgb(50,50,50)", "rgb(0,0,)"];
    // var z = Math.floor(Math.random() * 256);
    for (var i = 0; i < list.length; i++) {
        console.log(exe);
        document.body.style.background = exe; 
    }
}

colorChange();